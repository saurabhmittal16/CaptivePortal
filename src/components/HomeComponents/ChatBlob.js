import React from 'react';
import { Button } from 'antd';
class ChatBlob extends React.Component {
    render() {
        return (
            <Button className="chat-button">Chat with me</Button>
        );
    }
}

export default ChatBlob;